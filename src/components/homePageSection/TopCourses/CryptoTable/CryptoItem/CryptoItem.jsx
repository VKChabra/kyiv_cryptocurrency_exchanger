const CryptoItem = () => {
  return (
    <div>
      {/* {crypto.map(item => (
        <div key={item.name}>
          <p>{item.name}</p>
          <p>{item.last}</p>
          <p>{item.percentChange}</p>
          <p>{item.volume}</p>
        </div>
      ))} */}
    </div>
  );
};

export default CryptoItem;
